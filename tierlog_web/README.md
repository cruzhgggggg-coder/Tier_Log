# TierLog Web

Frontend web baru berbasis `Expo Router + React Native Web`.

## Menjalankan

1. Copy `.env.example` menjadi `.env`
2. Install dependency:
   - `npm.cmd install`
3. Jalankan:
   - `npm.cmd run dev`

Frontend akan memanggil backend Go pada `EXPO_PUBLIC_API_URL`.
